# 基础信息
title: Percival
description: 开发者 / 创作者 / 终身学习者
author: Percival

# 主题（保留用于文章功能，但样式已覆盖）
theme: minima

# 构建设置
markdown: kramdown
permalink: /:year/:month/:day/:title/
show_excerpts: false

# 分页（可选，文章多的时候启用）
# paginate: 10
# paginate_path: "/page:num/"

# 插件
plugins:
  - jekyll-feed
  - jekyll-seo-tag
  # - jekyll-paginate

# 排除文件
exclude:
  - README.md
  - backup/
